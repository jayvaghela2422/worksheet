// const mathjax = require("mathjax-node");

// const renderFormula = async (formula) => {
//   const math = mathjax.typeset({
//     math: formula,
//     format: "mml", // MathML output format for image generation
//   });

//   await math.promise; // Wait for MathJax to finish typesetting

//   const svg = math.mml; // Get the MathML code

//   // ... logic to convert MathML to image (optional)
//   // You can use libraries like `svg2png` or `puppeteer` for conversion

//   return svg; // Or return the converted image data
// };

// module.exports = renderFormula;
